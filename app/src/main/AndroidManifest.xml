<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.trailx">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,UnusedAttribute"
        android:usesCleartextTraffic="true">
        <activity android:name=".MusicScreen" />
        <activity android:name=".InAppMusicScreen" />
        <activity android:name=".TrailByTypeScreen" />
        <activity android:name=".TrailByDistanceScreen" />
        <activity android:name=".ActiveTrailScreen" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAXGNdRazpB1--duKAXCP4y4MEyXU9w7FU" />

        <activity android:name=".MyTrailsScreen" />
        <activity android:name=".EmergencySOSScreen" />
        <activity android:name=".DiscoverNewTrailsScreen" />
        <activity android:name=".SettingsScreen" />
        <activity android:name=".MyAccountScreen" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".RegisterScreen" />
        <activity android:name=".LoginScreen" />
        <activity
            android:name=".StartScreen"
            android:label="StartScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>